import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

/**
 * 9개 카테고리 3-티어 구조.
 * 각 카테고리의 _category_.json이 라벨·순서·link(index.md) 제공.
 * 루트 content/index.md는 sidebar_class_name: hidden + CSS로 사이드바에서 숨김.
 */
const sidebars: SidebarsConfig = {
  wikiSidebar: [
    {
      type: 'autogenerated',
      dirName: '.',
    },
  ],
};

export default sidebars;
